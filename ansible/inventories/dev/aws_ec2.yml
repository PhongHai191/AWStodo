plugin: amazon.aws.aws_ec2
regions:
  - ap-southeast-2

filters:
  tag:Role: web
  tag:Project: awstodo
  tag:Environment: dev
  instance-state-name: running

hostnames:
  - instance-id

groups:
  webservers: "tags.Role == 'web'"
